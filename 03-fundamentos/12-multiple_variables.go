package main

/*
var a, b, c int                       //variables del mismo tipo, se pueden declarar en una sola línea
var name, city = "Cristian", "España" //variables del mismo tipo, se pueden declarar en una sola línea, y se les asigna un valor inicial

var x, y = 10, 3.14159 //variables del mismo tipo, se pueden declarar en una sola línea, y se les asigna un valor inicial, el tipo de las variables se infiere automáticamente como int y float64 respectivamente

// bloque de variables relacionadas
var (
	age      int    = 30
	isActive bool   = true
	nickname string = "Cris"
)

func multipleVariables() {

	c, d := 5, 10 //short variable declaration, se asigna el valor 5 a la variable c y el valor 10 a la variable d, el tipo de las variables se infiere automáticamente como int

	full_name, ok := "Cristian", true //short variable declaration, se asigna el valor "Cristian" a la variable full_name y el valor true a la variable ok, el tipo de las variables se infiere automáticamente como string y bool respectivamente

	//no new variables on left side of :=compilerNoNewVar
	//c,d := 15,20

	//ahora no da error porque declaramos una nueva variable e, y las variables c y d ya existen, por lo que no se están redeclarando, sino que se les está asignando un nuevo valor
	c, d, e := 1, 5, 20
}
*/
